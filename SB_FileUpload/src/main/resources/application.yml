spring:
  datasource:
    username: root
    password: naveen@123
    url: jdbc:mysql://localhost:3306/movies
    driver: com.mysql.cj.jdbc.driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  
        
server:
  port: 9090
  
project:
  poster: posters/
